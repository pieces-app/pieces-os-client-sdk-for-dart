import 'package:pieces_os_client/api.dart';
import 'package:test/test.dart';

/// tests for AnnotationApi
void main() {
  // final instance = AnnotationApi();

  group('[Localhost] Tests for AnnotationApi', () {

    // /annotation/{annotation}/anchors/associate/{anchor} [POST]
    //
    // This will associate an anchor with an annotation.
    //
    test('Test /annotationAssociateAnchor Endpoint', () async {
    Future annotationAssociateAnchor( String annotation,  String anchor) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociateAnchor(annotation , anchor );
    });

    // /annotation/{annotation}/assets/associate/{asset} [POST]
    //
    // This will associate an asset with an annotation.
    //
    test('Test /annotationAssociateAsset Endpoint', () async {
    Future annotationAssociateAsset( String annotation,  String asset) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociateAsset(annotation , asset );
    });

    // /annotation/{annotation}/conversations/associate/{conversation} [POST]
    //
    // This will associate a conversation with an annotation.
    //
    test('Test /annotationAssociateConversation Endpoint', () async {
    Future annotationAssociateConversation( String annotation,  String conversation) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociateConversation(annotation , conversation );
    });

    // /annotation/{annotation}/messages/associate/{message} [POST]
    //
    // This will associate a message with an annotation.
    //
    test('Test /annotationAssociateConversationMessage Endpoint', () async {
    Future annotationAssociateConversationMessage( String annotation,  String message) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociateConversationMessage(annotation , message );
    });

    // /annotation/{annotation}/persons/associate/{person} [POST]
    //
    // This will associate a person with an annotation.
    //
    test('Test /annotationAssociatePerson Endpoint', () async {
    Future annotationAssociatePerson( String annotation,  String person) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociatePerson(annotation , person );
    });

    // /annotation/{annotation}/tags/associate/{tag} [POST]
    //
    // This will enable us to associate a tag with an annotation.
    //
    test('Test /annotationAssociateTag Endpoint', () async {
    Future annotationAssociateTag( String annotation,  String tag) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociateTag(annotation , tag );
    });

    // /annotation/{annotation}/websites/associate/{website} [POST]
    //
    // This will enable us to associate a website with an annotation.
    //
    test('Test /annotationAssociateWebsite Endpoint', () async {
    Future annotationAssociateWebsite( String annotation,  String website) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociateWebsite(annotation , website );
    });

    // /annotation/{annotation}/workstream_events/associate/{workstream_event} [POST]
    //
    // This will enable us to associate a workstream event with an annotation.
    //
    test('Test /annotationAssociateWorkstreamEvent Endpoint', () async {
    Future annotationAssociateWorkstreamEvent( String annotation,  String workstreamEvent) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociateWorkstreamEvent(annotation , workstreamEvent );
    });

    // /annotation/{annotation}/workstream_summaries/associate/{workstream_summary} [POST]
    //
    // This will enable us to associate a workstream summary with an annotation.
    //
    test('Test /annotationAssociateWorkstreamSummary Endpoint', () async {
    Future annotationAssociateWorkstreamSummary( String annotation,  String workstreamSummary) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationAssociateWorkstreamSummary(annotation , workstreamSummary );
    });

    // /annotation/{annotation}/anchors/disassociate/{anchor} [POST]
    //
    // This will enable us to dissassociate an anchor from an annotation.
    //
    test('Test /annotationDisassociateAnchor Endpoint', () async {
    Future annotationDisassociateAnchor( String annotation,  String anchor) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociateAnchor(annotation , anchor );
    });

    // /annotation/{annotation}/assets/disassociate/{asset} [POST]
    //
    // This will enable us to dissassociate an asset from an annotation.
    //
    test('Test /annotationDisassociateAsset Endpoint', () async {
    Future annotationDisassociateAsset( String annotation,  String asset) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociateAsset(annotation , asset );
    });

    // /annotation/{annotation}/conversations/disassociate/{conversation} [POST]
    //
    // This will enable us to dissassociate a conversation from an annotation.
    //
    test('Test /annotationDisassociateConversation Endpoint', () async {
    Future annotationDisassociateConversation( String annotation,  String conversation) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociateConversation(annotation , conversation );
    });

    // /annotation/{annotation}/messages/disassociate/{message} [POST]
    //
    // This will enable us to dissassociate a message from an annotation.
    //
    test('Test /annotationDisassociateConversationMessage Endpoint', () async {
    Future annotationDisassociateConversationMessage( String annotation,  String message) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociateConversationMessage(annotation , message );
    });

    // /annotation/{annotation}/persons/disassociate/{person} [POST]
    //
    // This will enable us to dissassociate a person from an annotation.
    //
    test('Test /annotationDisassociatePerson Endpoint', () async {
    Future annotationDisassociatePerson( String annotation,  String person) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociatePerson(annotation , person );
    });

    // /annotation/{annotation}/tags/disassociate/{tag} [POST]
    //
    // This will enable us to dissassociate a tag from an annotation.
    //
    test('Test /annotationDisassociateTag Endpoint', () async {
    Future annotationDisassociateTag( String annotation,  String tag) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociateTag(annotation , tag );
    });

    // /annotation/{annotation}/websites/disassociate/{website} [POST]
    //
    // This will enable us to dissassociate a website from an annotation.
    //
    test('Test /annotationDisassociateWebsite Endpoint', () async {
    Future annotationDisassociateWebsite( String annotation,  String website) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociateWebsite(annotation , website );
    });

    // /annotation/{annotation}/workstream_events/disassociate/{workstream_event} [POST]
    //
    // This will enable us to dissassociate a workstream event from an annotation.
    //
    test('Test /annotationDisassociateWorkstreamEvent Endpoint', () async {
    Future annotationDisassociateWorkstreamEvent( String annotation,  String workstreamEvent) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociateWorkstreamEvent(annotation , workstreamEvent );
    });

    // /annotation/{annotation}/workstream_summaries/disassociate/{workstream_summary} [POST]
    //
    // This will enable us to dissassociate a workstream summary from an annotation.
    //
    test('Test /annotationDisassociateWorkstreamSummary Endpoint', () async {
    Future annotationDisassociateWorkstreamSummary( String annotation,  String workstreamSummary) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationDisassociateWorkstreamSummary(annotation , workstreamSummary );
    });

    // '/annotation/{annotation}/scores/increment' [POST]
    //
    // This will take in a SeededScoreIncrement and will increment the material relative to the incoming body.
    //
    test('Test /annotationScoresIncrement Endpoint', () async {
    Future annotationScoresIncrement( String annotation, { SeededScoreIncrement? seededScoreIncrement }) async {
             returnable = .fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationScoresIncrement(annotation ,   seededScoreIncrement: seededScoreIncrement  );
    });

    // /annotation/{annotation} [GET]
    //
    // This will get a snapshot of a specific annotation.
    //
    test('Test /annotationSpecificAnnotationSnapshot Endpoint', () async {
    Future<Annotation ? > annotationSpecificAnnotationSnapshot( String annotation) async {
            Annotation?  returnable = Annotation.fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationSpecificAnnotationSnapshot(annotation );
    });

    // /annotation/update [POST]
    //
    // This will update a specific annotation.
    //
    test('Test /annotationUpdate Endpoint', () async {
    Future<Annotation ? > annotationUpdate({ Annotation? annotation }) async {
            Annotation?  returnable = Annotation.fromJson();
            print(returnable?.toJson());
            return returnable;
        }

        // Helpful Variables here for use with responses below
        // TODO - Need to check if it is nullable or not

        annotationUpdate(  annotation: annotation  );
    });
  });
}
